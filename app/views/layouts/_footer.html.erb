<footer class="footer container">
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <p class="navbar-text">Temp things...</p>
      <% if current_user %>
        <p class="navbar-text">
          Welcome, <%= current_user.name %>
        </p>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: ['btn', 'btn-warning', 'navbar-btn'] %>
      <% end %>
    </div>
  </nav>
</footer>
